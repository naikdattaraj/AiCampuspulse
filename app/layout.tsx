import './globals.css';
import { ReactNode } from 'react';
export const metadata={title:'AI CampusPulse – Assessments',description:'Dynamic student assessment platform'};
export default function RootLayout({children}:{children:ReactNode}){return <>{children}</>}
